pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                bash '#!/bin/bash
                      sudo echo docker ps -a echo'
            }
        }
    }
}
